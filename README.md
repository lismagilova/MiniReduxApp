# minireduxapp

таск-трекер на javascript: mvc, свой store и роутер. данные сохраняются в localStorage.

```sh
npm install
npm run dev
```

открыть http://localhost:8002.

тесты: `npm test`. сборка: `npm run build`.
